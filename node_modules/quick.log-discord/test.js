const log = require('./index.js')

log.options({
    "clientId": "346516120322179072",
    "clientSecret": "mCAkRqcw7cF3De6xGMcw73GYPbp9fkPu",
    "redirectUri": "http://127.0.0.1:8080/callback"
});

log.access(['144645791145918464', "145723964767862785"]);

setTimeout(function () {

    log.send('#Supports Markdown!')
    log.send('##Updates in real-time!')
    log.send('###Viewable from any browser!')

    // Counter
    let i = 0
    let flow = true;
    let func = setInterval(function () {
        var d = new Date();
        // Change Title
        log.title(`Last log: ${d.getHours()}:${d.getMinutes()}.${d.getMilliseconds()}`)

        // This right here switches the flow every time a message is sent
        if (flow) log.flow('up'), flow = false;
        else log.flow('down'), flow = true;

        // Send Message
        log.send(`**${i}** | *Random #* = \`${Math.random()}\``)
        log.send(`<button type="button" id="test">Pixels Why is there a button here?</button>`)

        i++
    }, 500)


}, 1500)
